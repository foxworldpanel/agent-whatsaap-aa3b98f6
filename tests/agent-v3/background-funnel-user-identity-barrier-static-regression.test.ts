import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260914543000_background_funnel_user_identity_barrier.sql","utf8");describe("background Funnel full routing identity barrier",()=>{it("resolves user from the exact conversation/workspace",()=>expect(sql).toContain("c.id=p_conversation_id AND c.workspace_id=p_workspace_id"));it("fails closed when durable conversation routing is missing",()=>expect(sql).toContain("IF NOT FOUND OR v_user_id IS NULL THEN RETURN true"));it("blocks same-workspace user mismatch as well as active/review Funnel",()=>{expect(sql).toContain("s.user_id IS DISTINCT FROM v_user_id");expect(sql).toContain("s.status IN('running','needs_review')");});});
