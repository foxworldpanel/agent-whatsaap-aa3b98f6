import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260914511500_stage_b_claim_funnel_workspace_identity.sql","utf8");describe("Stage B Funnel workspace fence",()=>{it("uses shared routing barrier in exhausted and ordinary scans",()=>{expect((sql.match(/has_welcome_funnel_agent_barrier/g)||[]).length).toBeGreaterThanOrEqual(6);expect(sql).toContain("j.workspace_id");});it("revalidates after canonical nonblocking fence",()=>{const lock=sql.indexOf("pg_try_advisory_xact_lock(hashtextextended(v_candidate.conversation_id::text,31))");expect(lock).toBeGreaterThan(0);expect(sql.indexOf("has_welcome_funnel_agent_barrier(v_candidate.conversation_id,v_candidate.workspace_id)",lock)).toBeGreaterThan(lock);});it("retains real max-attempt bound",()=>{expect(sql).toContain("j.attempt_count>=p_max_attempts");expect(sql).toContain("j.attempt_count<p_max_attempts");expect(sql).not.toContain("2147483647");});});
