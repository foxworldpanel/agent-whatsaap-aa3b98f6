import { describe,expect,it } from "vitest";import fs from "node:fs";import path from "node:path";
function collectTsFiles(dir:string):string[]{return fs.readdirSync(dir,{withFileTypes:true}).flatMap(entry=>{const full=path.join(dir,entry.name);if(entry.isDirectory())return collectTsFiles(full);return entry.isFile()&&/\.tsx?$/.test(entry.name)?[full]:[];});}
describe("Agent V3 source-of-truth",()=>{
 it("does not retain obsolete global commercial canned-reply configuration",()=>{const root=path.resolve(process.cwd(),"src/lib/agent-v3");const source=collectTsFiles(root).map(file=>fs.readFileSync(file,"utf8")).join("\n");expect(source).not.toContain("VERBOSE_LOOP_FAREWELL");expect(source).not.toContain("GLOBAL_V3_CONFIG");});
 it("keeps the canonical panel URL only in explicit customer-facing routing surfaces",()=>{const root=path.resolve(process.cwd(),"src/lib/agent-v3");const hits=collectTsFiles(root).filter(file=>fs.readFileSync(file,"utf8").toLowerCase().includes("mindsmmpanel.com")).map(file=>path.relative(root,file).replace(/\\/g,"/"));expect(hits.length).toBeGreaterThan(0);for(const hit of hits){expect(["orchestrator.server.ts","prompt/prompt-conditional.server.ts"]).toContain(hit);}});
});
