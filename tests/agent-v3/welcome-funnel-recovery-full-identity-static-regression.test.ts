import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260914533000_welcome_funnel_recovery_full_identity.sql","utf8");describe("Welcome Funnel recovery full identity",()=>{it("uses canonical nonblocking conversation fence",()=>expect(sql).toContain("pg_try_advisory_xact_lock(hashtextextended(v.conversation_id::text,31))"));it("captures complete durable identity and exact stale timestamp",()=>{expect(sql).toContain("s.user_id,s.workspace_id,s.updated_at");expect(sql).toContain("s.user_id=v.user_id AND s.workspace_id=v.workspace_id");expect(sql).toContain("s.updated_at=v.updated_at");});it("quarantines before exact stale lock deletion",()=>{const review=sql.indexOf("SET status='needs_review'");const del=sql.indexOf("DELETE FROM public.agent_generation_locks");expect(review).toBeGreaterThan(-1);expect(del).toBeGreaterThan(review);expect(sql).toContain("g.holder=v_lock.holder AND g.acquired_at=v_lock.acquired_at");});});
