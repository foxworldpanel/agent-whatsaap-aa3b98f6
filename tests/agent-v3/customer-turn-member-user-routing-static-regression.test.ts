import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260914534500_customer_turn_member_user_routing_fence.sql","utf8");describe("Customer Turn member user routing fence",()=>{it("uses canonical seed-31 conversation fence",()=>expect(sql).toContain("hashtextextended(v_conversation_id::text,31)"));it("resolves user from exact durable job conversation/workspace",()=>expect(sql).toContain("c.id=v_conversation_id AND c.workspace_id=v_workspace_id"));it("rejects mixing an existing turn user with a reassigned conversation user",()=>{expect(sql).toContain("v_turn_user_id IS DISTINCT FROM v_user_id");expect(sql).toContain("turn user routing identity mismatch");});it("extends Funnel attachment barrier to user identity",()=>expect(sql).toContain("s.user_id IS DISTINCT FROM v_user_id"));});
