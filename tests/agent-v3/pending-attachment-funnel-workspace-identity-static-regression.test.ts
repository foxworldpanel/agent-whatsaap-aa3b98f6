import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260914513000_pending_attachment_funnel_workspace_identity.sql","utf8");describe("pending attachment Funnel workspace identity",()=>{it("filters discovery through shared barrier",()=>{expect(sql).toContain("NOT public.has_welcome_funnel_agent_barrier(j.conversation_id,j.workspace_id)");});it("rechecks after seed-31 lock and per durable job",()=>{expect(sql).toContain("pg_try_advisory_xact_lock(hashtextextended(v_conversation.conversation_id::text,31))");expect(sql).toContain("has_welcome_funnel_agent_barrier(v_job.conversation_id,v_job.workspace_id)");});it("refuses cross-workspace aggregation",()=>{expect(sql).toContain("v_turn_workspace IS DISTINCT FROM v_job.workspace_id");expect(sql).toContain("Pending attachment Customer Turn workspace mismatch");});});
