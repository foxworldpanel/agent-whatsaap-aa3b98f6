import { describe,expect,it } from "vitest";import fs from "node:fs";import { detectConversationContext } from "../../src/lib/agent-v3/selector/module-selector.server";
const runtime=fs.readFileSync("src/lib/agent-v3/runtime.server.ts","utf8");const executionContext=fs.readFileSync("src/lib/agent-v3/core/agent-execution-context.server.ts","utf8");const p1=fs.readFileSync("src/lib/agent-v3/prompt/prompt-p1.server.ts","utf8");const p2=fs.readFileSync("src/lib/agent-v3/prompt/prompt-p2.server.ts","utf8");
describe("Atendimento consultivo e humano",()=>{
 it("entende objetivo de engajamento",()=>{const ctx=detectConversationContext("Eu gostaria de engajar minha música",[]);expect(ctx.hasGrowthGoal).toBe(true);expect(ctx.intent).toBe("descoberta");expect(ctx.stage).toBe("apresentacao")});
 it("prioriza dúvida factual antes da oferta",()=>{expect(p1).toContain("Pergunta factual");expect(p1).toContain("tem prioridade sobre empurrar preço")});
 it("permite quebrar explicação em mensagens",()=>{expect(p2).toContain("===SPLIT===");expect(p2).toContain("divide em 2-3 mensagens")});
 it("consome no runtime o Customer Turn já agregado pelo Stage C",()=>{expect(runtime).toContain("const effectiveAgentMessage = finalMsgText");expect(runtime).toContain("buildCustomerTurnRuntimeInput() agrega, ordena e resolve todos os membros");expect(runtime).not.toContain("burstBodies.join")});
 it("decisão comercial usa exatamente a mensagem agregada no shared execution context",()=>{expect(runtime).toContain("message: effectiveAgentMessage");expect(runtime).toContain("buildAgentExecutionContext({");expect(executionContext).toContain("deriveBusinessDecisionV3({");expect(executionContext).toContain("message,");});
});