import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260914530000_pending_attachment_workspace_quarantine.sql","utf8");describe("pending attachment routing quarantine",()=>{it("serializes on canonical conversation fence",()=>expect(sql).toContain("hashtextextended(v_conversation.conversation_id::text,31)"));it("quarantines corrupt collecting turn instead of raising forever",()=>{expect(sql).toContain("v_turn_workspace IS DISTINCT FROM v_job.workspace_id");expect(sql).toContain("state='needs_review'");expect(sql).not.toContain("RAISE EXCEPTION 'Pending attachment Customer Turn workspace mismatch'");});it("quarantines already attached pending members but preserves unattached job for a clean turn",()=>{expect(sql).toContain("FROM public.agent_customer_turn_messages tm WHERE tm.turn_id=v_turn AND tm.job_id=j.id");expect(sql).toContain("IF v_turn IS NULL THEN INSERT INTO public.agent_customer_turns");expect(sql).toContain("VALUES(v_job.conversation_id,v_job.workspace_id)");});});
