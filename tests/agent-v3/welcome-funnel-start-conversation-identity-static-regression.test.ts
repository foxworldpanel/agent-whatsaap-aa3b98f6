import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260914514500_welcome_funnel_start_conversation_identity_fence.sql","utf8");describe("Welcome Funnel start conversation identity",()=>{it("keeps exact running retry idempotent",()=>{expect(sql).toContain("IF v_status='running' THEN RETURN true");});it("blocks durable Funnel routing mismatch",()=>{expect(sql).toContain("s.workspace_id IS DISTINCT FROM p_workspace_id");expect(sql).toContain("s.user_id IS DISTINCT FROM p_user_id");});it("blocks Agent workspace mismatch before active-runtime test",()=>{const mismatch=sql.indexOf("Agent routing identity mismatch");const runtime=sql.indexOf("active Agent runtime");expect(mismatch).toBeGreaterThan(0);expect(mismatch).toBeLessThan(runtime);});it("retains exact holder and seed 31",()=>{expect(sql).toContain("holder=p_holder");expect(sql).toContain("hashtextextended(p_conversation_id::text,31)");});});
