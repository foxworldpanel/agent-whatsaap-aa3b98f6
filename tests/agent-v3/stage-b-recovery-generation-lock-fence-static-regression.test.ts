import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260914541500_stage_b_recovery_ownership_separation.sql","utf8");describe("Stage B recovery ownership separation",()=>{it("keeps canonical seed-31 fairness fence",()=>{expect(sql).toContain("pg_try_advisory_xact_lock(hashtextextended(v_candidate.conversation_id::text,31))");expect(sql).toContain("LIMIT 500");expect(sql).toContain("v_locked>=100");});it("preserves safe and uncertain recovery semantics",()=>{expect(sql).toContain("j.status='processing_safe'");expect(sql).toContain("THEN 'needs_review' ELSE 'pending'");expect(sql).toContain("j.status='processing'");expect(sql).toContain("external side effect uncertain");});it("does not mutate generation locks",()=>expect(sql).not.toMatch(/DELETE\s+FROM\s+public\.agent_generation_locks/i));});
