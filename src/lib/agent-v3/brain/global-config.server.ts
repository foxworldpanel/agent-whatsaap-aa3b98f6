/**
 * Legacy compatibility stub.
 *
 * Agent V3 commercial configuration must come from the CMS/runtime modules,
 * never from hardcoded URLs or offers in source code.
 *
 * This file intentionally exports nothing. It is kept as a tombstone so that
 * repository updates performed by copying files cannot accidentally resurrect
 * the old hardcoded configuration.
 */
export {};
